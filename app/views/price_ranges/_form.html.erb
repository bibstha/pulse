<%= form_for(price_range) do |f| %>
  <% if price_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(price_range.errors.count, "error") %> prohibited this price_range from being saved:</h2>

      <ul>
      <% price_range.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.datetime_select :end_date %>
  </div>

  <div class="field">
    <%= f.label :vendor_id %>
    <%= f.text_field :vendor_id %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
